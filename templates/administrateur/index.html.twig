{% extends 'base.html.twig' %}

{% block title %}Aministrateur index{% endblock %}

{% block body %}
    <h1>Administrateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Login</th>
                <th>Mot_de_passe</th>
                <th>Roles</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for administrateur in administrateur %}
            <tr>
                <td>{{ administrateur.id }}</td>
                <td>{{ administrateur.login }}</td>
                <td>{{ administrateur.motDePasse }}</td>
                <td>{{ administrateur.roles ? administrateur.roles|json_encode : '' }}</td>
                <td>
                    <a href="{{ path('app_administrateur_show', {'id': aministrateur.id}) }}">show</a>
                    <a href="{{ path('app_administrateur_edit', {'id': administrateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_administrateur_new') }}">Create new</a>
{% endblock %}
