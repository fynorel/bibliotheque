<form method="post" action="{{ path('app_aministrateur_delete', {'id': aministrateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aministrateur.id) }}">
    <button class="btn">Delete</button>
</form>
