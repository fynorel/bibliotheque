<form method="post" action="{{ path('app_copain_delete', {'id': copain.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ copain.id) }}">
    <button class="btn">Delete</button>
</form>
